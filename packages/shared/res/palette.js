import colors from "./colors";

const palette = {
  heading: {
    color: colors.main.default,
    fontSize: 20,
    textAlign: "center"
  },
  text: {
    color: colors.blue.default,
    fontSize: 17,
    textAlign: "center"
  }
};

export default palette;
